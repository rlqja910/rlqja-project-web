spring:
  main:
    lazy-initialization: true
  application:
    name: dashboard-app
  
  datasource:
    url: jdbc:postgresql://ep-fancy-frost-azklg9ou-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_u60UzqENSawH
    driver-class-name: org.postgresql.Driver

    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          time_zone: Asia/Seoul
        temp:
          use_jdbc_metadata_defaults: false

# 나중에 파이썬 워커 및 프론트엔드와 통신할 포트 설정
server:
  port: 8080

app:
  python-worker:
    url: http://localhost:8000
